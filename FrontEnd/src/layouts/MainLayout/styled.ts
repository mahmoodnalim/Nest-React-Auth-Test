import { styled } from "@mui/material";

export const StyledContainer = styled('div')(() => ({
    display: "flex",
    flex: 1,
    flexDirection: 'row',
    height: '100%',
    justifyContent: 'start',
    alignItems: 'start'
}));
